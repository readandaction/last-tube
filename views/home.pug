extends layouts/main
include mixins/videoBlock 

block content
    .videos
        each video in videos
            +videoBlock({
                title:video.title,
                videoFile:video.videoFile,
                views:video.views
            })